# 阶段 8：ComfyUI 可选适配器

日期：2026-08-26  
状态：完成适配器与协议测试；未捆绑第三方工作流、节点或模型

## 本阶段交付

- 增加 `comfyui` 统一工作流后端，不在主进程导入 ComfyUI 代码。
- 对接官方 `/system_stats`、`/upload/image`、`/prompt`、`/history/{id}`、`/view`、`/queue` 和 `/history` 路由。
- 强制回环 HTTP 地址、节点类允许清单、许可证已审查声明和禁止网络节点声明。
- 每个任务使用独立交换子目录，并在结束后清理 ComfyUI 输入、输出和历史记录。
- 对下载结果执行 25 MB 上限、路径引用、解码和目标尺寸验证，再由本应用重写水印与 AI 元数据。
- 提供不含真实节点或模型的工作流包模板和逐步配置文档。

## 验证边界

自动化测试使用可控的进程外协议替身验证完整数据流和清理行为。由于仓库不分发 ComfyUI、自定义换脸节点或模型，本阶段不把任何具体第三方工作流宣称为已授权或已通过真人质量验收。

## 下一阶段

阶段 9 将完成 Windows 一键初始化、启动、诊断、用户手册、第三方声明生成和干净环境复现说明。
