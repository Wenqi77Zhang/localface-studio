# 阶段 11：成熟度与发布安全加固

日期：2026-08-27
状态：工程完成，等待集中人工验收

## 本阶段交付

- 修复后端重启后未过期结果仍占空间但无法访问的问题。
- 新增最长 24 小时、可续期的 SQLite 本地匿名会话；原始 Cookie 不落盘。
- 新增处理能力就绪状态和稳定提示码，前端可解释模型缺失、模拟模式、首次加载和 CPU 降级。
- 将页面陈旧的“阶段 4”标识更新为 `v1 候选版`，任务结束后自动刷新真实运行能力。
- 完成 npm 生产依赖与 Python 当前环境漏洞审计，均未发现已知漏洞。
- 增加 Dependabot 与 CodeQL，覆盖 uv、npm、GitHub Actions、Python 和 TypeScript。
- Dependabot 仅按生态合并创建 minor/patch 建议并忽略 major 自动升级，避免一次产生大量高风险更新。
- 普通启动由 Vite 开发服务切换为已检查的生产构建预览，并为页面与代理增加 CSP 和安全响应头。
- 完成成熟度审计、公网部署门禁和独立安全边界 ADR。

## 关键边界

本阶段没有把本地服务部署到公网。原因不是技术上无法绑定端口，而是当前研究模型许可证、账户隔离、滥用治理和法律审查均未满足生产门禁。维持阻止公网绑定是本阶段的安全成果。
