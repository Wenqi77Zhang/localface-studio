# 阶段 14：前端工具更新与状态副作用收敛

日期：2026-08-29
状态：工程完成，等待集中人工验收

## 本阶段交付

- 单独引入已通过远端检查的前端开发工具小版本更新，不触碰换脸模型、许可证、任务数据或公网边界。
- 将 Vite 更新至 8.2.2、React 插件更新至 6.1.0、Oxlint 更新至 1.80.0，并同步类型定义与锁文件。
- 新检查器暴露的 7 个 React 副作用警告全部处置：可派生的状态不再重复写入；确属浏览器文件 URL 或外部检测请求同步的状态保留局部说明，不全局关闭规则。
- 结果对比组件按任务编号重新挂载，避免上一任务的滑杆或弹窗状态泄漏到下一任务。
- 本地结果过期时直接从派生状态关闭大图，不再用额外副作用追赶结果列表。
- 修复浏览器截图验证复用固定 Edge 配置目录导致残留锁文件误报失败的问题；每次验证使用独立临时目录并自动清理。

## 验证

- 前端严格安装、Oxlint 零警告、TypeScript 和 Vite 生产构建通过。
- 真实 Microsoft Edge 以 1440×1000 打开生产预览，页面布局、上传框、授权区、高级设置、节点流和本地结果区均无新增错位或溢出。
- 完整 Python、前端、启动、代理、资产与 Git 历史质量门通过。

## 保持不变的边界

- Python/CUDA 13 跨代更新未合并，仍需独立 GPU 回归。
- 当前版本仍是本地非商业研究候选版，不创建稳定标签，也不开放公网处理接口。
