# 阶段 2 进展日志：模拟推理产品闭环

状态：进行中（策划已冻结）  
开始日期：2026-07-23  

## 阶段目标

在不下载或运行人脸模型的前提下，完成可实际操作的单图产品闭环，验证上传、授权、任务队列、实时事件、节点可视化、结果比较、下载和清理等工业化边界。

## 已冻结决策

| 主题 | 决策 |
|---|---|
| 模拟结果 | 复制目标图并添加醒目的“SIMULATION—非真实换脸结果”标识 |
| 上传格式 | PNG、JPEG、静态 WebP |
| 上传限制 | 单图 25 MB、5000 万像素、最长边 12000 px |
| 本地会话 | 随机会话、CSRF 防护、Host/Origin 校验 |
| 账号扩展 | 使用 `actor_id` 预留账号绑定和最小化授权审计 |
| 实时事件 | SSE 推送；HTTP 负责创建、取消、下载和删除 |
| 任务状态 | 排队、处理、成功、失败、取消、超时、过期、删除 |
| 工作流节点 | 文件校验、任务准备、模拟处理、输出检查、导出结果 |
| 输出格式 | PNG 默认，可选 JPEG；JPEG 质量 5–100，默认 95 |
| 输出标记 | 可见水印默认开启、允许关闭；AI/模拟元数据始终写入 |
| 结果比较 | 滑杆、并排模式、同步缩放和平移 |
| 默认保留 | 30 分钟 |
| 可选保留 | 1、3、6、12 或 24 小时；24 小时为硬上限 |
| 任务记录 | SQLite 最小化记录；图片使用随机隔离目录 |
| 并发策略 | 单任务执行；每个会话最多三个未完成任务 |
| 故障测试 | 仅自动化测试和默认关闭的开发模式可用 |

## 隐私与安全约束

- 每个任务必须重新确认图片授权和合法用途。
- 授权回执只保存会话或账号标识、确认时间、声明版本和任务编号。
- 本地版不记录 IP，不在日志中记录图片内容、原始文件名、绝对路径、令牌或人脸特征。
- 原始图、目标图和结果图仅存在于当前随机任务目录。
- 文件扩展名、媒体类型和解码后的真实格式必须一致。
- 拒绝损坏图片、动画或多帧图片、超限图片、压缩炸弹和路径穿越。
- 失败、取消和超时立即清理任务图片；启动时清理过期遗留任务。
- 模拟输出必须同时包含可见模拟标识和 `simulation=true` 元数据。

## 实施顺序

- [x] 建立阶段 2 任务契约、状态机、后端接口和 SQLite 存储边界。
- [x] 实现本地安全会话、CSRF 与请求来源校验。
- [x] 实现流式上传、内容级图片校验和隔离任务目录。
- [x] 实现单并发队列、取消、超时和 SSE 事件。
- [x] 实现启动时的过期任务与遗留文件清理。
- [x] 实现可替换模拟后端、PNG/JPEG 导出、水印与元数据。
- [ ] 实现前端上传、授权、节点状态、取消、结果比较、下载和删除。
- [ ] 补齐成功、失败、取消、超时、清理、越权和恶意输入测试。
- [ ] 运行统一质量门禁、公开仓库扫描和目标设备人工验收。

## 阶段退出门禁

- [ ] 模拟成功、失败、取消和超时路径均通过自动化测试。
- [ ] 过期清理、越权访问、路径穿越和恶意图片均通过安全测试。
- [ ] 前端任务与节点状态和后端 SSE 事件一致。
- [ ] 刷新页面后可恢复有效任务，或明确说明任务已经失效。
- [ ] PNG/JPEG 输出尺寸、可见标识、AI 元数据和水印策略通过验证。
- [ ] 所有终止路径不产生可下载的伪成功结果，并按策略完成清理。
- [ ] 产品负责人在目标设备上完成阶段 2 人工验收。

## 已完成记录

- [x] 2026-07-23：定义排队、处理、成功、失败、取消、超时、过期和删除状态及其合法转换。
- [x] 2026-07-23：定义阶段 2 工作流节点、PNG/JPEG 输出格式和最小任务记录。
- [x] 2026-07-23：建立与 SQLite 解耦的 `TaskRepository` 应用层协议。
- [x] 2026-07-23：实现 SQLite 建表、所属会话隔离查询、未完成任务计数和过期结果查询。
- [x] 2026-07-23：使用任务版本号和原子条件更新防止并发状态覆盖。
- [x] 2026-07-23：验证数据库连接在成功与异常路径均会提交、回滚并关闭。
- [x] 2026-07-23：后端完整质量门禁通过，共 19 项测试，分支覆盖率 92.53%。
- [x] 2026-07-23：实现只存在于后端内存的随机会话、`actor_id` 和 CSRF 凭据。
- [x] 2026-07-23：使用 HttpOnly、SameSite=Strict、会话期 Cookie 建立本地浏览器会话。
- [x] 2026-07-23：限制会话存储容量并采用最近最少使用淘汰，防止无界内存增长。
- [x] 2026-07-23：实现 Host、Origin、`Sec-Fetch-Site` 和变更请求 CSRF 校验。
- [x] 2026-07-23：验证其他 Host、跨站请求、错误来源、缺失令牌和重启后的旧会话均被拒绝。
- [x] 2026-07-23：会话与请求防护的 8 项定向测试及 Ruff、Mypy 检查通过。
- [x] 2026-07-23：质量门禁为每次运行创建独立随机测试目录并禁用非必要缓存，避免不同 Windows 执行权限复用临时目录。
- [x] 2026-07-23：产品负责人在普通 PowerShell 环境确认完整前后端质量门禁通过。
- [x] 2026-07-23：锁定 Pillow 12.3.0（MIT-CMU）和 python-multipart 0.0.32（Apache-2.0），并更新许可证清单。
- [x] 2026-07-23：实现 64 KiB 分块读取、单图 25 MB 限制和空文件拒绝。
- [x] 2026-07-23：实现扩展名、媒体类型、真实编码格式、完整解码、帧数、像素数和最长边校验。
- [x] 2026-07-23：使用随机任务编号和固定角色文件名隔离存储，不把原始文件名写入路径。
- [x] 2026-07-23：任一图片失败、磁盘异常或异步取消时回滚整个任务工作区。
- [x] 2026-07-23：使用程序生成的非人物图片完成 15 项上传核心定向测试。
- [x] 2026-07-23：产品负责人确认新增依赖与上传核心通过完整前后端质量门禁。
- [x] 2026-07-23：新增受 Host、Origin、会话与 CSRF 防护的 `POST /api/v1/tasks` 接口。
- [x] 2026-07-23：任务创建接口强制逐次授权确认，并由后端写入当前授权声明版本。
- [x] 2026-07-23：实现 PNG/JPEG 输出选项、水印选项以及 30 分钟、24 小时和 7 天保留期限（后于 2026-07-24 收紧）。
- [x] 2026-07-23：在 multipart 解析前要求可信 `Content-Length`，并限制两张图片加表单开销的请求总大小。
- [x] 2026-07-23：串行化本地任务创建，确保每个会话最多三个未完成任务的判断不会并发越界。
- [x] 2026-07-23：接口响应和 SQLite 记录均不包含原始文件名、磁盘路径或公开的 `actor_id`。
- [x] 2026-07-23：任务接口的 4 组集成测试通过，覆盖成功、授权、回滚、限流与请求大小门禁。
- [x] 2026-07-23：产品负责人确认任务上传 HTTP 接口通过完整前后端质量门禁。
- [x] 2026-07-23：建立与具体模型解耦的 `WorkflowBackend` 和单并发异步任务队列。
- [x] 2026-07-23：实现运行节点递进、成功、受控失败、内部失败、取消和超时状态收敛。
- [x] 2026-07-23：失败、取消和超时均调用工作区清理，成功任务保留结果目录。
- [x] 2026-07-23：建立有界任务事件历史和订阅缓冲，为 SSE 接口提供统一事件源。
- [x] 2026-07-23：队列核心的 4 组异步测试通过，验证最大并发数始终为 1。
- [x] 2026-07-23：产品负责人确认单并发队列核心通过完整前后端质量门禁。
- [x] 2026-07-23：实现不使用模型、不改变人物身份的诚实模拟后端，源图仅参与完整性校验，输出像素来自目标图。
- [x] 2026-07-23：PNG/JPEG 结果保持目标图尺寸，并始终写入 `simulation=true`、AI 编辑标记、后端类型和处理时间元数据。
- [x] 2026-07-23：模拟结果始终带有不可关闭的醒目标识；普通 AI 编辑水印默认开启但允许用户关闭。
- [x] 2026-07-23：输出采用固定文件名与临时文件原子替换，元数据不包含会话标识、原始文件名或绝对路径。
- [x] 2026-07-23：模拟后端和队列的 6 项定向测试通过，覆盖 PNG、JPEG、水印开关、元数据、尺寸、节点顺序和输入缺失。
- [x] 2026-07-23：产品负责人确认模拟后端通过完整前后端质量门禁。
- [x] 2026-07-23：应用生命周期负责启动和停止单并发任务队列，任务创建成功后自动进入模拟工作流。
- [x] 2026-07-23：新增任务状态查询、取消、SSE 进度、结果下载和删除接口。
- [x] 2026-07-23：查询不到的任务与其他会话拥有的任务统一返回 `404`，避免泄露任务是否存在。
- [x] 2026-07-23：仅成功任务允许下载，运行中任务必须先取消，终止任务删除时同步清理隔离工作区。
- [x] 2026-07-23：任务控制接口的 6 项定向测试通过，覆盖执行成功、事件顺序、下载、越权隔离、取消和删除。
- [x] 2026-07-23：产品负责人确认任务执行与控制接口通过完整前后端质量门禁。
- [x] 2026-07-23：应用启动时将重启遗留的排队或运行任务收敛为超时，并删除对应图片工作区。
- [x] 2026-07-23：启动时清理已到期结果、异常遗留的终止任务文件和没有数据库记录的规范任务目录。
- [x] 2026-07-23：未到期的成功结果在重启后继续保留，应用运行期间每 60 秒执行一次到期检查。
- [x] 2026-07-23：清理服务采用最小文件系统协议并对存储异常失败关闭，不静默跳过隐私清理错误。
- [x] 2026-07-23：启动恢复与定时清理的 2 项定向测试通过，覆盖遗留任务、孤立目录、到期结果、保留结果和及时停止。
- [x] 2026-07-23：产品负责人确认自动清理功能通过完整前后端质量门禁。
- [x] 2026-07-24：前端建立受 Host、Origin、会话 Cookie 与 CSRF 保护的本地 API 会话。
- [x] 2026-07-24：前端实现身份来源图与目标场景图的本地选择、完整预览和同步自适应画框，不把图片内容上传到第三方。
- [x] 2026-07-24：前端实现逐次授权确认、输出格式、保留期限、水印选项与受控任务提交。
- [x] 2026-07-24：前端通过 SSE 显示文件校验、任务准备、模拟处理、输出检查与安全导出五个真实工作流节点。
- [x] 2026-07-24：缺失图片或授权时仅提供本地可访问性提示，不调用后端；任务终止三秒后自动清空节点状态。
- [x] 2026-07-24：产品负责人确认前端上传、授权、提交与实时节点进度符合预期。
- [x] 2026-07-24：前端实现运行中任务取消，取消请求锁定防止重复提交，并以真实终止状态收敛界面。
- [x] 2026-07-24：运行中任务除 SSE 外每 500 毫秒回查一次本地状态，避免事件流中断造成虚假卡顿。
- [x] 2026-07-24：修复 Windows 图片线程尚未释放文件时取消任务可能导致工作器退出的问题，终止清理增加有限重试。
- [x] 2026-07-24：单个任务发生未预期异常时记录脱敏错误并继续处理队列，不再连带阻塞后续任务。
- [x] 2026-07-24：队列、取消、控制接口与模拟后端 8 项定向测试通过，产品负责人确认成功与取消路径符合预期。
- [x] 2026-07-24：将新任务保留期限收紧为 30 分钟、1、3、6、12 或 24 小时，24 小时为硬上限，已有任务到期时间不追溯缩短。
- [x] 2026-07-24：新增 JPEG 质量 5–100 参数、默认值 95、SQLite 自动迁移与服务端边界校验。
- [x] 2026-07-24：输出格式、JPEG 质量、本地保留和水印集中到授权确认后的折叠式高级设置板块。
- [x] 2026-07-24：高级设置采用纵向雾面玻璃布局，并提供随折叠状态变化的方向箭头和 JPEG 默认质量刻度。
- [x] 2026-07-24：成功结果通过本地受保护接口加载，独立结果组件提供完整预览与固定安全文件名下载。
- [x] 2026-07-24：高级参数、迁移、模拟导出和任务接口 25 项定向测试通过，产品负责人确认高级设置与独立结果预览符合预期。
- [x] 2026-07-24：结果组件提供目标场景原图与模拟结果的可拖动分割线对比，并支持触摸、方向键、Home 和 End 操作。
- [x] 2026-07-24：对比画面保持目标图比例与完整显示，分割线采用与工作台一致的低干扰深色工业风，产品负责人完成视觉和交互验收。
- [x] 2026-07-24：新增当前匿名会话隔离的未过期结果列表接口，仅返回随机任务编号、完成时间、到期时间和输出格式，并使用 `no-store` 禁止缓存。
- [x] 2026-07-24：结果列表只包含真实存在的成功结果文件，按完成时间倒序排列，不暴露原始文件名、磁盘路径或其他会话记录。
- [x] 2026-07-24：左侧本地结果组件采用双列、统一横向圆角黑底缩略图和内部滚动，图片均等比例完整显示。
- [x] 2026-07-24：大图弹窗支持关闭按钮、遮罩和 Esc 退出；超大图片同时受弹窗宽高约束，不裁切、不拉伸且不显示滚动条。
- [x] 2026-07-24：单项与全部清除均要求页面内二次确认，通过 CSRF 防护接口同步清理结果和关联上传副本；已下载到其他位置的文件不受影响。
- [x] 2026-07-24：本地结果图库每 15 秒同步其他标签页，剩余时间不足 10 分钟时提示，到期后自动移出界面。
- [x] 2026-07-24：产品负责人确认本地结果图库、大图预览、下载与主动清除交互符合预期。

## 当前进展

阶段 2 的任务创建、执行、控制、自动清理和主动清除后端闭环已经完成；前端上传、逐次授权、高级设置、任务提交、节点进度、任务取消、结果对比、本地图库、大图预览、下载和清除也已通过人工验收。尚未下载或运行任何换脸模型。完成最终质量门禁后进入阶段 3：人脸检测与目标选择。

## 变更控制

阶段 2 的冻结范围可以通过后续架构决策调整，但涉及联网、账号身份收集、IP 记录、持久化人脸数据、放宽保存期限或公开部署的变更，必须先更新隐私威胁模型并由产品负责人明确确认。
